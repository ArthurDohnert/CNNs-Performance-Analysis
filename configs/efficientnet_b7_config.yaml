# configs/efficientnet_b7_config.yaml
# Configuração para os experimentos finais com o modelo EfficientNet B7 no PCAD.

model_params:
  num_classes: 200

train_params:

  epochs: 100
  batch_size: 8
  learning_rate: 0.001

inference_params:
  batch_size: 8